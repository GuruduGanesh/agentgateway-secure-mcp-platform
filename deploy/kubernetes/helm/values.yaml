# Intentionally empty for agentgateway v1.4.0.
# Gateway Services are created and owned by the control plane; local access uses
# kubectl port-forward in tests/smoke/smoke-k8s.ps1.  The former gateway.service
# block was not a v1.4.0 chart value and Helm silently ignored it.
{}
